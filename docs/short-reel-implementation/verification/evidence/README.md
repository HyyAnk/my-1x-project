# Evidence Records

No product execution evidence exists yet. This folder holds future per-phase implementation and review reports. Documentation-kit validation is not product implementation evidence.

Use `phase-01-implementation.md` through `phase-08-implementation.md`, and `phase-01-review.md` through `phase-08-review.md`. Record additional attempts with a numbered suffix rather than erasing previous failed evidence. Link the latest authoritative attempt from progress.md.

Screenshots/logs may be adjacent under phase-specific directories if concretely claimed and zone-covered. Keep logs concise and redacted. Do not store tokens, keys, cookies, raw private bank datasets or generated footage without user authorization. Large media can remain at user-approved artifact paths referenced by the report.

Commands must include timestamp, cwd, exit code and test counts where available. Distinguish real HTTP/storage/UI workflows from provider stubs and actual manual Flow runs. Check evidence freshness against the current product diff; old screenshots and old green tests cannot prove current behavior.

Evidence and progress edits happen before claim verification. Registry release is confirmed by the subsequent reviewer; do not edit after successful verification merely to append the release result.
