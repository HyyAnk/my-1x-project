# Execute the Reviewed Short-Reel Repair Plan

Work in D:/1a Cursor Project/My 1x Project on the actual main checkout. Implement only the next eligible repair stage from docs/superpowers/plans/2026-09-07-short-reel-phase-02-03-repair.md. Read that entire plan and its linked specification/contracts/review before editing.

Read AGENTS.md and all coordination/Short-Reel startup documents. Capture current git status and registry status. Reproduce the relevant findings against current code; preserve unrelated dirty changes. Do not assume previous success messages are acceptance.

Stage selection:

1. If Stage A has no released implementation plus independent fresh review or explicit user/integrator acceptance, execute Stage A only: repair the Phase 02 source/persistence boundary and prove writer safety. Stop after implementation evidence, verification and release. Request fresh review; do not self-accept or advance to B.
2. If Stage A is independently accepted with current evidence, execute Stage B only: repair Phase 03 contracts, selection, topic projection, slot validation, HTTP/UI workflow and evidence. Stop after released implementation and request fresh Phase 03 re-review.
3. If implementation awaits review, report the reviewer prompt and do not start downstream implementation. If either stage is rejected, resume that stage's bounded repairs under a new claim.

Use the plan's exact tasks and red/green assertions. Discover all affected consumers before changing contracts. Acquire authenticated concrete-file claims, expand before extra edits and never store lease tokens in files. Do not create branches/worktrees, auto-spawn agents, commit, operate live Flow, call paid providers, mutate the real bank, migrate/reset existing records, remove portrait assets or delete/archive the kit without separate explicit authority.

The proposed writer policy requires a demonstrated supported locking primitive, not a PID file or per-instance Map. Stop for integrator approval if new runtime/dependency authority is needed. Existing incomplete snapshots must be preserved and reported; do not reconstruct historical provenance from today's bank. Keep all new text English and leave the footer conflict explicit.

For testing, use isolated temporary storage and stub only external providers. Use actual routes/repositories/UI. Do not reuse unknown default Playwright servers at 2244/4310; create a test-owned isolated harness on free ports. Exercise restart, concurrency, source fidelity, failure/retry and 1440/390/320px keyboard/touch behavior. Save and inspect screenshots. Rebuild/restart after final changes.

Keep the original rejection report. Write numbered repair evidence and unique handoffs, reconcile F03-10 without inventing past permission, and update progress honestly. Run required checks, verify and release the claim with current evidence; do not edit after verification. Acceptance is performed by a fresh reviewer/user-integrator, and final product acceptance remains with the user.

Final response: completed stage, findings resolved/unresolved, concrete files, observed tests and workflow results, remaining authority decisions, evidence links, claim release status and the next eligible review prompt. Do not launch Phase 04 automatically.
