# Antigravity Resume Prompt

Resume the Short-Reel upgrade in `D:/1a Cursor Project/My 1x Project` using `docs/short-reel-upgrade/`.

Read `AGENTS.md`, the pack README, approved design, contracts, implementation plan, and `execution/PROGRESS.md`, `DECISIONS.md`, `EVIDENCE.md`. Read the acceptance matrix and the current phase file completely. The product direction is approved; do not redesign it or restart finished phases.

Inspect the current git diff and compare it with the recorded implementation ownership. Preserve unrelated user changes. Re-run the last relevant checkpoint test so you know which code is actually working, then continue from the exact next unchecked step.

Follow test-first implementation, immutable reel storage, provider reference-byte conditioning, portrait requests, title/description publishing, acyclic dependencies and automatic per-unit UI refresh. Never use a fake episode, a silent placeholder or an unsupported text-only reference fallback to claim success.

Update progress/evidence after each completed phase. Do not automatically commit, deploy, post, change live provider configuration, delete data or perform new paid verification. Confirm that any existing live budget approval still covers the remaining calls before continuing live work. If approval is absent, stop at that gate and report precisely what remains.

Begin with a short evidence-backed status: last completed phase, latest verified checks, current failure if any, and next exact step.
