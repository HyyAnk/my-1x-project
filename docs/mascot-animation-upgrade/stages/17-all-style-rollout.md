# Stage 17 — All-style generation

## Objective

Plan and process every existing style with bounded concurrency. Publish style by style only after twenty ready slots.

## Completion evidence

Evidence: batch report, failed slots visible, retryable errors and no silent skips.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

