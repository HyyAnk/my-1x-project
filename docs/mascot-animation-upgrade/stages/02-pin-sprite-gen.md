# Stage 02 — Pin sprite-gen and prerequisites

## Objective

Resolve the installed sprite-gen version, upstream revision, Codex provider availability, virtual environment and artifact paths. Add diagnostics only.

## Completion evidence

Evidence: version report and fixture command shape. Stop if twelve frames cannot be represented.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

