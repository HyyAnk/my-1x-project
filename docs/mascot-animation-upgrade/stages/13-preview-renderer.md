# Stage 13 — Preview renderer integration

## Objective

Use the shared frame-at-time resolver for Studio and HTML preview. Verify frame boundaries, loop, geometry, pivot and localized assets.

## Completion evidence

Evidence: snapshot parity at frame boundaries and mid-frame times.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

