# Stage 10 — Server API and progress

## Objective

Add route schemas, thin routes and task/event progress integration. Verify success, slow, failure, retry, cancellation, reconnect and stale events.

## Completion evidence

Evidence: route contract tests and structured error responses.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

