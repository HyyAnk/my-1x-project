# Stage 06 — Manifest parser and importer

## Objective

Parse the pinned manifest, follow declared frame paths and rectangles, validate fingerprints, and derive registration, pivot and bounds.

## Completion evidence

Evidence: importer fixtures; reject guessed grids, stale revisions and unsafe paths.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

