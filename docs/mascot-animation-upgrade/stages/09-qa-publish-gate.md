# Stage 09 — QA and publish gate

## Objective

Implement alpha, bounds, duplicate, motion, seam, semantic and manifest checks. Require curation status where configured.

## Completion evidence

Evidence: publish rejects any style with fewer than twenty ready slots.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

