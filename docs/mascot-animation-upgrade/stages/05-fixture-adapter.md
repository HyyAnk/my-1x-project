# Stage 05 — Fixture sprite-gen adapter

## Objective

Build the process adapter behind an interface. Add deterministic fixture mode with atlas, manifest and reports without credentials.

## Completion evidence

Evidence: timeout, abort, non-zero exit, missing output and structured-log tests.

## Required report

Record changed files, commands, test output, artifacts, open risks and the next stage. Do not change a decision in the parent plan without updating the plan first.

