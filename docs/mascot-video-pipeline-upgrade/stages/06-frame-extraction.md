# Stage 06 — Frame extraction

## Objective

Add FFmpeg adapter with timeout, abort, metadata capture, sequential frame output and fixture mode.

## Completion evidence

Evidence: success, partial output, cancellation and timeout tests.

## Required report

Record changed files, commands, evidence, open risks and the next stage. Stop before the next stage if any contract or check is unresolved.

