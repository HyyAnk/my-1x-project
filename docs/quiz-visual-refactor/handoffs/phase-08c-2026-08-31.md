# Phase 8C Handoff: Production/Sandbox Parity and Visual Evidence

Date: 2026-08-31
Status: COMPLETE
Branch: main
Baseline HEAD: `595fc3fef332c80bfb511d72dd3c2514d3ce6be4`
Repository state: cumulative uncommitted Phase 2–8C working tree; no files staged, committed, reverted, deleted, or absorbed outside Phase 8C scope

## Outcome

Phase 8C gives production and Visual Sandbox one canonical semantic background contract, bundles only the background CSS used by each composition, and replaces string-only visual conclusions with browser-inspected and manually reviewed render evidence.

- Both surfaces emit `.quiz-scene-background` with the same variant markup and stable `data-background-style` ID for equivalent inputs.
- Production derives used background IDs from resolved questions; Sandbox supplies its one selected ID. Registry order makes output deterministic, deduplicated, and selected-only.
- A local Playwright generator renders 16 PNGs covering four layouts × two aspect ratios × two backgrounds. It serves fonts and mascot art locally, seeks to a real reveal frame, pauses browser animations, checks computed style and geometry, and writes a reviewable JSON/Markdown manifest.
- The running application proves pending, success, error, retry, rapid latest-request-wins, Channel PATCH/read-back synchronization without F5, desktop/mobile overflow, exact responsive footer copy, keyboard focus, 44 px touch targets, touch input, and reduced-motion behavior.
- CodeGraph was available and used before direct reads to trace shared scene parts, the background registry/renderer, CSS assembly, production and Sandbox composition paths, preview lifecycle, and browser entry points.

No external provider or network asset is used by Phase 8C evidence. No layout, background, skin, palette, choice count, foreground-motion ownership, four-choice migration, legacy-adapter cleanup, or broad redesign was added.

## Acceptance coverage

| Matrix case   | Evidence                                                                                                                                                                                                                       | Result |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| `P8C-PAR-01`  | `quizPhase08cParity.test.ts` calls both public composition builders for `candy_rays` and `aurora_glow` and requires the exact canonical semantic layer from each path.                                                         | PASS   |
| `P8C-PAR-02`  | The same suite verifies selected-only CSS, registry-order deduplication, one shared base selector, and omission of unused or legacy duplicate blocks.                                                                          | PASS   |
| `P8C-PAR-03`  | Repeated composition output is byte-deterministic; browser inspection requires static background descendants and a state-preserving reduced-motion mascot window.                                                              | PASS   |
| `P8C-VIS-01`  | `artifacts/phase-08c/` contains 16 inspected PNGs for all four layouts, 16:9 and 9:16, and both backgrounds.                                                                                                                   | PASS   |
| `P8C-VIS-02`  | The matrix distributes all four Answer Card skins, production/Sandbox surfaces, mascot on/off, long text, and visual choices without an unnecessary Cartesian expansion.                                                       | PASS   |
| `P8C-VIS-03`  | `manifest.json` records surface, input ID, style IDs, viewport/aspect, mascot state, output path, every browser inspection field, reviewer, result, and `null` caveat.                                                         | PASS   |
| `P8C-UI-01`   | Playwright checks the running application at its desktop viewport and 390×844 mobile viewport, no horizontal overflow, concise title copy, and exactly one required responsive footer variant.                                 | PASS   |
| `P8C-ASY-01`  | Hook tests and Playwright prove immediate pending feedback, success, 503 error, retry, rapid background changes, stale-response rejection, loader completion, and no-F5 synchronization.                                       | PASS   |
| `P8C-A11Y-01` | Component/browser tests prove the named layout combobox, keyboard focus/selection, named and pressed background buttons, dialog semantics, accessible close control, 44 px targets, touch `.tap()`, and reduced-motion output. | PASS   |
| `P8C-E2E-01`  | `quizPhase08c.spec.ts` uses the real preview route, browser input, Channel PATCH plus refetch, and confirmed iframe output; the layer under test is not bypassed.                                                              | PASS   |

## Production and UI fixes proven by browser evidence

1. Added `semanticBackgroundLayer.ts`; background variants now own only their inner semantic content and use identical production/Sandbox wrappers.
2. Added selected-only background CSS assembly and passed resolved used IDs through production and Sandbox composition entry points.
3. Wrapped the Sandbox font-readiness contract in an executable `<script>` element; the real browser had rejected the previous raw text insertion.
4. Preserved the previous confirmed preview while a request is pending, cleared superseded pending data, cleared pending state on the latest error, exposed the actual error, and retained a deterministic retry path.
5. Added responsive Sandbox layout CSS, mobile document scrolling, narrower fit zoom, accessible/touch-sized controls, and modal dialog semantics without changing visual architecture.
6. Browser artifact inspection caught portrait min-content overflow in `media_left_choices_right`; its 9:16 grid now uses `minmax(0, 1fr)`.
7. Browser artifact inspection caught portrait visual-choice overflow and phase/choice occlusion; the portrait image height is bounded and the 9:16 game stage reserves phase-overlay space.
8. Reduced-motion CSS had collapsed every timed mascot state window to one millisecond. Motion remains static, but state-window duration is now preserved so the correct mascot frame remains visible.

## Visual artifacts

- Matrix index: `docs/quiz-visual-refactor/artifacts/phase-08c/README.md`
- Machine-readable manifest: `docs/quiz-visual-refactor/artifacts/phase-08c/manifest.json`
- Outputs: 16 PNGs under `docs/quiz-visual-refactor/artifacts/phase-08c/`
- Automated inspection: background fills stage; content remains inside stage; document has no overflow; mascot and phase overlays do not occlude choices/content; mascot on/off state matches the manifest; all three choices are visible; fonts are ready; reduced-motion descendants are static.
- Manual review: every final PNG was opened and inspected for clipping, overflow, hierarchy, long-text truncation behavior, choice visibility, background coverage, phase placement, and mascot visibility. No caveat was accepted.

## Files changed

Production and tests:

- `apps/server/src/quiz/visual/elements/background/semanticBackgroundLayer.ts`
- `apps/server/src/quiz/visual/elements/background/registry.ts`
- `apps/server/src/quiz/visual/elements/background/types.ts`
- `apps/server/src/quiz/visual/elements/background/variants/candyRays.ts`
- `apps/server/src/quiz/visual/elements/background/variants/auroraGlow.ts`
- `apps/server/src/quiz/render/candyArcadeComposition.ts`
- `apps/server/src/quiz/render/candyArcade/candyArcadeStyles.ts`
- `apps/server/src/quiz/render/sandboxComposition.ts`
- `apps/server/src/quiz/render/layouts/mediaLeftChoicesRight.ts`
- `apps/server/src/quiz/render/layouts/visualChoicesThree.ts`
- `apps/server/test/quizPhase08cParity.test.ts`
- `apps/server/test/quizBackgroundRegistry.test.ts`
- `apps/server/test/quizVisualContractsCharacterization.test.ts`
- `apps/server/tsconfig.eslint.json`

Artifact tooling:

- `apps/server/scripts/phase08cArtifacts.types.ts`
- `apps/server/scripts/phase08cArtifacts.fixtures.ts`
- `apps/server/scripts/generatePhase08cArtifacts.ts`

Running UI and browser evidence:

- `apps/web/src/components/composition-preview/CompositionPreviewFrame.tsx`
- `apps/web/src/features/sandbox/VisualSandboxTab.tsx`
- `apps/web/src/features/sandbox/components/SandboxChannelSyncModal.tsx`
- `apps/web/src/features/sandbox/components/SandboxDesignTab.tsx`
- `apps/web/src/features/sandbox/components/SandboxHeader.tsx`
- `apps/web/src/features/sandbox/components/SandboxPreviewCanvas.tsx`
- `apps/web/src/features/sandbox/components/design/SandboxLayoutSelector.tsx`
- `apps/web/src/features/sandbox/components/design/SandboxStyleOptionSection.tsx`
- `apps/web/src/features/sandbox/components/preview/SandboxMonitorHeader.tsx`
- `apps/web/src/features/sandbox/hooks/useSandboxPreviewRenderer.ts`
- `apps/web/src/features/sandbox/hooks/useSandboxPreviewRenderer.test.tsx`
- `apps/web/src/features/sandbox/hooks/useSandboxViewportState.ts`
- `apps/web/src/styles/features/sandbox.css`
- `apps/web/src/styles/index.css`
- `apps/web/test/phase08cAppFixture.ts`
- `apps/web/test/quizPhase08c.spec.ts`

Dossier and evidence:

- `docs/quiz-visual-refactor/README.md`
- `docs/quiz-visual-refactor/compatibility-matrix.md`
- `docs/quiz-visual-refactor/roadmap-status.md`
- `docs/quiz-visual-refactor/phases/phase-08-end-to-end-stabilization.md`
- `docs/quiz-visual-refactor/phases/phase-08c-interaction-plan.md`
- `docs/quiz-visual-refactor/artifacts/phase-08c/`
- `docs/quiz-visual-refactor/handoffs/phase-08c-2026-08-31.md`

## Verification

Targeted and production-adjacent workflows:

- `pnpm --filter @studio/server exec vitest run test/quizPhase08cParity.test.ts test/quizBackgroundRegistry.test.ts test/quizVisualContractsCharacterization.test.ts test/sandboxComposition.test.ts` — PASS, 44/44.
- `pnpm --filter @studio/web exec vitest run src/features/sandbox/hooks/useSandboxPreviewRenderer.test.tsx src/features/sandbox/hooks/useSandboxChannelSync.test.tsx src/features/sandbox/components/design/SandboxBackgroundSelector.test.tsx src/features/sandbox/components/design/SandboxLayoutSelector.test.tsx src/components/AppViewRouter.test.tsx` — PASS, 25/25. The ErrorBoundary exception on stderr is deliberate test input.
- `pnpm --filter @studio/web exec playwright test test/quizPhase08c.spec.ts --workers=1` — PASS, 1/1.
- `pnpm exec tsx apps/server/scripts/generatePhase08cArtifacts.ts` — PASS, 16/16 local Chromium renders and inspections; final unchanged run completed in 7.80 seconds after documentation formatting.

Workspace gates:

- `pnpm format:check` — PASS.
- `pnpm lint` — PASS.
- `pnpm typecheck` — PASS.
- `pnpm test` — PASS on required full rerun: server 538/538, web 76/76, choice audit 0 violations. The first parallel run timed out only in the previously documented `quizV2Route` 5-second test; its isolated rerun passed 2/2 in 511 ms and the unchanged full rerun passed.
- `pnpm build` — PASS; shared/server compile and Vite production bundle complete, with all chunks below 500 kB.
- `pnpm test:e2e` — PASS on required full rerun, 12/12. The first parallel run raced an existing shared engine fixture and failed one Dashboard model-name assertion; the isolated test passed 1/1 and the unchanged full rerun passed.
- `pnpm audit:quiz-choices` — PASS, 0 violations.
- `pnpm exec prettier --check "docs/quiz-visual-refactor/**/*.md"` — PASS on final dossier run.
- `git diff --check` — PASS on final run.

Post-update workflows were rebuilt/restarted by the Playwright web-server configuration and rerun through the real preview route. The final production-adjacent artifact workflow rebuilt both production and Sandbox compositions and browser-inspected their outputs; completion is not based on compile or string assertions alone.

## Working-tree preservation and deviations

Preflight recorded branch `main`, full HEAD, and the complete `git status --short`. The task confirmed no competing agent was using this working tree. The cumulative Phase 2–8B implementation and previously recorded unrelated paths were preserved. In particular, Phase 8C did not stage, commit, revert, delete, or absorb unrelated changes such as `apps/server/src/quiz/visual/candyArcade.ts`, `apps/server/test/mascotStudio.test.ts`, `apps/web/src/features/sandbox/hooks/useSandboxPresets.ts`, and `eslint-suppressions.json`.

The only verification deviations were the two existing parallel-load/fixture races documented above; both passed in isolation and in unchanged required full reruns. No acceptance criterion was downgraded and no visual PASS was inferred from strings.

## Remaining risks and readiness

- The cumulative implementation remains uncommitted and must be preserved by the next task.
- Phase 8D still owns the bounded decision for `legacyBackgroundAdapter.ts`, canonical layout-dimension cleanup, structure review, stale suppressions/dead exports, and final dossier closure.
- The artifact fixture uses deterministic local SVG fallback art, so it proves layout and semantic rendering, not provider-generated illustration quality.
- Existing full suites can approach fixed timeouts or share mutable E2E fixture state under parallel load; required clean reruns passed without production changes.

Phase 8D is READY. Phase 8 remains incomplete until 8D closes its acceptance matrix. The separate four-choice project remains DEFERRED.
