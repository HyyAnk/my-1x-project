/**
 * Animation Types Re-export Barrel
 *
 * Deconstructed into modular sub-types under ./types/:
 * - manifest.types.ts: Animation manifest, atlas, frame rects, playback targets
 * - videoJob.types.ts: MascotVideoProcessingJob, QA reports, revisions
 * - slotAnimation.types.ts: Slot state, slot projection, prompt context
 * - batch.types.ts: Animation batches, jobs, attempts, recipes
 */

export * from "./types/index.js";
