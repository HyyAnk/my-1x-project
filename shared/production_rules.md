# Production Rules

- Keep every artifact readable outside the dashboard.
- Do not begin quiz generation, director planning, or asset/voice production before a topic is confirmed.
- Never let a generated path escape its scoped repository directory.
