# Production Rules

- Keep every artifact readable outside the dashboard.
- Do not begin research, script, or scene work before a topic is confirmed.
- Never let a generated path escape its scoped repository directory.
