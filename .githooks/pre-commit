#!/bin/sh
# Cooperative commit gate: blocks staged files owned by unreleased active
# claims and warns on files matching no zone. See scripts/coordination/commit-gate.mjs.
node "$(dirname "$0")/../scripts/coordination/commit-gate.mjs"
